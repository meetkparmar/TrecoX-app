<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bebetterprogrammer.trecox">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/trecox_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/trecox_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrecoX">
        <activity android:name=".ui.ProductOrderActivity"
            android:theme="@style/Widget.App.NoActionBar" />
        <activity android:name=".ui.CompanyDetailActivity"
            android:theme="@style/Widget.App.NoActionBar"/>
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.OtpVerificationActivity" />
        <activity
            android:name=".ui.OnBoardingActivity"
            android:theme="@style/Widget.App.NoActionBar" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/Widget.App.SplashScreenNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>